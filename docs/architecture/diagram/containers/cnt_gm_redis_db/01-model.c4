model {
    extend greenhouse_system {
        cnt_gm_redis_db = cache "CNT_GM_Redis_DB" {
            description "Кэш последних показаний датчиков (пишет SavingService по потоку с брокера); сессии; читает WebAPI"
            technology "Redis"
        }
    }
}